# Windows Desktop Engine for Frosty WOS AI
This directory contains the native Windows C++ desktop runner and CMake build definitions for Windows 10/11 x64.
To compile on Windows:
```powershell
cd device/mobile
flutter build windows --release
```
The output binary will be generated at `device/mobile/build/windows/x64/runner/Release/frosty_wos_ai.exe`.
