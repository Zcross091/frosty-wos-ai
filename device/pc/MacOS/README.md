# macOS Desktop Engine for Frosty WOS AI
This directory contains the native Apple Silicon & Intel macOS desktop configurations and entitlement rules.
To compile on macOS:
```bash
cd device/mobile
flutter build macos --release
```
The output app bundle will be generated at `device/mobile/build/macos/Build/Products/Release/frosty_wos_ai.app`.
